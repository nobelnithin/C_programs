#include <stdio.h>

int main(){
    int number;
    scanf("%d",&number);
    if(number%2==0)
         printf("divisible by 2");
    else if(number=0){
        printf("Not divisible by 2");
    }
    else
         printf("Not divisible by 2");

}