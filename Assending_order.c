#include <stdio.h>

main(void){
    int N,i,min;
    printf("Enter the value of N: ");
    scanf("%d",&N);
    int array1[N],array[];
    printf("Enter the Elements: ");
    for(i=1;i<=N;i++){
        printf("Enter the Element %d",i);
        scanf("%d",&array1[i]);
    }
    min=1;
    for(i=0;i<N;i++){
        if(array[i]<min){
             
        }
    }

}